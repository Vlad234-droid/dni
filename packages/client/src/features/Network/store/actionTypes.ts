const ROOT = 'networks';
const GET_LIST_ACTION = `${ROOT}/list`;
const GET_ONE_ACTION = `${ROOT}/one`;
const SET_ONE_ACTION = `${ROOT}/set_one`;
const GET_COUNT_ACTION = `${ROOT}/count`;
const GET_PARTICIPANTS_ACTION = `${ROOT}/participants`;

export {
  ROOT,
  GET_LIST_ACTION,
  GET_ONE_ACTION,
  SET_ONE_ACTION,
  GET_COUNT_ACTION,
  GET_PARTICIPANTS_ACTION,
};
