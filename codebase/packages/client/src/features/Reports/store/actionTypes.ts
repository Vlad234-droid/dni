const ROOT = 'reports';
const GET_REPORTS_BY_TIME = `${ROOT}/list/by-time`;
const GET_REPORTS_BY_REGION = `${ROOT}/list/by-region`;
const GET_REPORTS_BY_FORMAT = `${ROOT}/list/by-format`;
const GET_NETWORKS = `${ROOT}/list/networks`;
const GET_EVENTS = `${ROOT}/list/events`;

export { ROOT, GET_REPORTS_BY_TIME, GET_REPORTS_BY_REGION, GET_REPORTS_BY_FORMAT, GET_NETWORKS, GET_EVENTS };
